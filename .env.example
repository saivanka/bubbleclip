# Copy to .env and fill in what you need. Everything is optional.

# Pin your own access code (otherwise one is generated on first run
# and printed in the container logs). Use ACCESS_CODE=disabled to run
# without auth on a fully trusted network.
#ACCESS_CODE=

# Only needed for docker-compose.tailscale.yml
#TS_AUTHKEY=tskey-auth-xxxxx
